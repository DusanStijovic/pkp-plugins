{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Listen For Xdebug",
      "type": "php",
      "request": "launch",
      "port": 9003,
      "pathMappings": {
        "/var/www/html": "${workspaceFolder}",
        "/var/www/files": "${workspaceFolder}/volumes/private"
      }
    }
  ]
}
