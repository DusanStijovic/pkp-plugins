{
  "name": "ojs-plugins-workspace",
  "private": true,
  "scripts": {
    "test:e2e": "cypress run --headless --browser chrome",
    "test:e2e:ci": "bash -lc 'python3 -m http.server 4173 >/tmp/ojs-cypress-http.log 2>&1 & SERVER_PID=$!; trap \"kill $SERVER_PID\" EXIT; cypress run --headless --browser chrome'",
    "test:e2e:ceonHelpCenter": "cypress run --headless --browser chrome --config '{\"specPattern\":[\"plugins/generic/ceonHelpCenter/tests/cypress/functional/*.cy.js\"]}'"
  },
  "devDependencies": {
    "cypress": "^15.13.1"
  }
}
